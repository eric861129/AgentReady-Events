# AgentReady Events v3 rules

- Follow the approved v3 plan and daily topic; do not add unplanned product features.
- Day 1–12 executable teaching work belongs in `labs/`; formal product work starts at Day 13.
- Use TDD for every feature or behavior change.
- Keep exactly the five approved Tool names.
- Registration and cancellation finalization remain human UI actions.
- Only `src/client/webmcp/adapter.ts` may access `document.modelContext` in the formal product.
- Never describe Fake ModelContext or direct Tool execution as real Codex discovery.
- Do not bulk-delete files or directories.
- Do not push or replace a remote without explicit author approval.
